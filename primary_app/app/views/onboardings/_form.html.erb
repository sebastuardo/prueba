<%= form_with(model: onboarding, local: true) do |form| %>
  <% if onboarding.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(onboarding.errors.count, "error") %> prohibited this onboarding from being saved:</h2>

      <ul>
      <% onboarding.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :datail %>
    <%= form.text_field :datail %>
  </div>

  <div class="field">
    <%= form.label :offers_id %>
    <%= form.text_field :offers_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
